version: 2
services:
  - type: web
    name: carbattle-server
    env: node
    buildCommand: npm install
    startCommand: node server/index.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 18
